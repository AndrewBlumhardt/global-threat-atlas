window.MAP_CONFIG = {
  subscriptionKey: "PASTE_YOUR_AZURE_MAPS_KEY"
};

// Set to true when anonymous/public blob access is disabled on the storage account.
// Skips the anonymous probe and routes all data requests through the Function App.
// window.USE_FUNCTION_API_ONLY = true;
