# map

Azure Maps SDK initialisation.

## Files

| File | Description |
|---|---|
| `map-init.js` | Creates and returns the Atlas map control using the subscription key received from `/api/config`. Configures base tile style, camera bounds, and event listeners. |
